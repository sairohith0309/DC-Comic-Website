

<p>
 <% if user_signed_in? %>
 <h1>Account page</h1>

<h4>Profile Details<h4>

Name:
<%= "#{current_user.name}" %>
</p>
<p>
phone number:
<%= "#{current_user.phoneno}" %>
</p>
<p>
email:
<%= "#{current_user.email}" %>
</p>

<p>
address:
<%= "#{current_user.address}" %>
</p>

 
    <% end %>

<% if user_signed_in? %>
<%= link_to 'Payment information',editpay_path, class:'btn btn-primary' %>

<% else %>
<h3> Requried Sign in </h3><br>

 <%= link_to 'Sign In', new_user_session_path, class:'me-md-2' %>
 <%= link_to 'Sign Up', new_user_registration_path %>
 <% end %>